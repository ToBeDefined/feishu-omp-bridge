diff --git a/es/index.js b/es/index.js
index 8d6e87e616d8a2a8fece170433d8ea906e2edaed..2b0e6edba0a714b6e4fca9b63aa15f5350349705 100644
--- a/es/index.js
+++ b/es/index.js
@@ -89921,6 +89921,12 @@ class Throttle {
      * or do nothing (a fire is already scheduled).
      */
     note(deltaChars) {
+        // feishu-omp-bridge patch: a timer-fired flush that rejects used to
+        // die as an unhandledRejection (fireSoon voids doFire's promise) and
+        // the stream kept hammering the API with the same failing snapshot.
+        // Capture it and surface it to the producer on the next note/flush.
+        if (this.lastError)
+            throw this.lastError;
         this.pendingChars += deltaChars;
         if (this.pendingChars >= this.opts.chars) {
             this.fireSoon(0);
@@ -89937,7 +89943,9 @@ class Throttle {
             clearTimeout(this.timer);
         this.timer = setTimeout(() => {
             this.timer = undefined;
-            void this.doFire();
+            this.doFire().catch((e) => {
+                this.lastError = e;
+            });
         }, delay);
     }
     /**
@@ -89949,6 +89957,8 @@ class Throttle {
      */
     flushNow() {
         return __awaiter(this, void 0, void 0, function* () {
+            if (this.lastError)
+                throw this.lastError;
             if (this.timer) {
                 clearTimeout(this.timer);
                 this.timer = undefined;
@@ -89971,6 +89981,7 @@ class Throttle {
                 this.pendingChars = 0;
                 this.lastFireAt = Date.now();
                 yield this.fire();
+                this.lastError = undefined;
             }))();
             this.inFlight = p;
             try {
diff --git a/lib/index.js b/lib/index.js
index 9bceeb22a63f774a26c22857f9be66599186f8e8..0ef1413eb25a591e37b8b887ec58ca054feb5c2c 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -89939,6 +89939,12 @@ class Throttle {
      * or do nothing (a fire is already scheduled).
      */
     note(deltaChars) {
+        // feishu-omp-bridge patch: a timer-fired flush that rejects used to
+        // die as an unhandledRejection (fireSoon voids doFire's promise) and
+        // the stream kept hammering the API with the same failing snapshot.
+        // Capture it and surface it to the producer on the next note/flush.
+        if (this.lastError)
+            throw this.lastError;
         this.pendingChars += deltaChars;
         if (this.pendingChars >= this.opts.chars) {
             this.fireSoon(0);
@@ -89955,7 +89961,9 @@ class Throttle {
             clearTimeout(this.timer);
         this.timer = setTimeout(() => {
             this.timer = undefined;
-            void this.doFire();
+            this.doFire().catch((e) => {
+                this.lastError = e;
+            });
         }, delay);
     }
     /**
@@ -89967,6 +89975,8 @@ class Throttle {
      */
     flushNow() {
         return __awaiter(this, void 0, void 0, function* () {
+            if (this.lastError)
+                throw this.lastError;
             if (this.timer) {
                 clearTimeout(this.timer);
                 this.timer = undefined;
@@ -89989,6 +89999,7 @@ class Throttle {
                 this.pendingChars = 0;
                 this.lastFireAt = Date.now();
                 yield this.fire();
+                this.lastError = undefined;
             }))();
             this.inFlight = p;
             try {
